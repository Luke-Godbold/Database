The folders in frontend will need to replace the public and source in a react app called frontend. 
The backend folder needs to be in the same folder as the react frontend folder.

You need to start app.py before starting the react app.
